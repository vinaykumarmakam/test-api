apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-product.fullname" . }}
  labels:
    {{- include "data-product.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-product.selectorLabels" . | nindent 4 }}
